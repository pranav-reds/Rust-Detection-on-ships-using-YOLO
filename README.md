# Rust-Detection-on-ships-using-YOLO
This is a project in which YOLO is used to detect the rust which is present on ships. the specific model used is YOLOv11, The model achieves an decent accuracy with only limited training and can achieve a greater accuracy when provided with more training. 
